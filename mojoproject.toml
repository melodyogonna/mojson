[project]
channels = ["conda-forge", "https://conda.modular.com/max"]
description = "A simple JSON Parser for Mojo"
name = "mojson"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
max = ">=24.6.0,<25"
